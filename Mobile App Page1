<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        /* Reset some default styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Set a background color and text color */
        body {
            background-color: #f0f0f0;
            font-family: Arial, sans-serif;
        }

        /* Style the header */
        header {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 1rem 0;
        }

        /* Style the main content */
        main {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 2rem;
        }

        /* Style the container for text content */
        .container {
            flex: 1;
            padding: 1rem;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        .container h2 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        .container p {
            font-size: 1rem;
            color: #777;
        }

        /* Style the button */
        .btn {
            display: inline-block;
            padding: 0.5rem 1rem;
            background-color: #333;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #555;
        }

        /* Style the image container */
        .image-container {
            flex: 1;
            padding: 1rem;
            text-align: center;
        }

        /* Style the footer */
        footer {
            text-align: center;
            padding: 1rem 0;
            background-color: #333;
            color: #fff;
        }
    </style>
    <title>Smart Lighting App</title>
</head>
<body>
    <header>
        <h1>Welcome to the SmartLight Pro App</h1>
    </header>
    <main>
        <section class="container">
            <h2>Control Your Lights</h2>
            <p>Manage and control the lighting in your building with ease.</p>
            <br>
            <a href="#" class="btn">Get Started</a>
        </section>
        <section class="image-container">
            <img src="b.jpg" alt="Smart Lighting" style="width: 800px; height: auto;">
        </section>
    </main>
    <footer>
        <p>&copy; 2023 Smart Lighting Solutions presented by Kashish</p>
    </footer>
</body>
</html>
